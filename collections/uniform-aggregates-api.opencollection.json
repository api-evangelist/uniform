{
  "opencollection": "1.0.0",
  "info": {
    "name": "Uniform Platform Aggregates API",
    "version": "2.0"
  },
  "items": [
    {
      "info": {
        "name": "Aggregates",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "GET /api/v2/aggregate",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "{{baseUrl}}/api/v2/aggregate",
            "params": [
              {
                "name": "projectId",
                "value": "",
                "type": "query",
                "description": "The project to fetch aggregates for"
              },
              {
                "name": "aggregateId",
                "value": "",
                "type": "query",
                "description": "Limit the results to a specific aggregate ID"
              },
              {
                "name": "type",
                "value": "",
                "type": "query",
                "description": "Limit the results to a specific aggregate type\n0: Audience\n1: Intent\n"
              }
            ],
            "auth": {
              "type": "apikey",
              "key": "x-api-key",
              "value": "{{x-api-key}}",
              "placement": "header"
            }
          }
        },
        {
          "info": {
            "name": "PUT /api/v2/aggregate",
            "type": "http"
          },
          "http": {
            "method": "PUT",
            "url": "{{baseUrl}}/api/v2/aggregate",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "apikey",
              "key": "x-api-key",
              "value": "{{x-api-key}}",
              "placement": "header"
            }
          }
        },
        {
          "info": {
            "name": "DELETE /api/v2/aggregate",
            "type": "http"
          },
          "http": {
            "method": "DELETE",
            "url": "{{baseUrl}}/api/v2/aggregate",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "apikey",
              "key": "x-api-key",
              "value": "{{x-api-key}}",
              "placement": "header"
            }
          }
        },
        {
          "info": {
            "name": "OPTIONS /api/v2/aggregate",
            "type": "http"
          },
          "http": {
            "method": "OPTIONS",
            "url": "{{baseUrl}}/api/v2/aggregate"
          },
          "docs": "Handles preflight requests. This endpoint allows CORS."
        }
      ]
    }
  ],
  "bundled": true
}